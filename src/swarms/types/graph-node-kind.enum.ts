export enum GraphNodeKind {
  WORKER = 'worker',
  IF_ELSE = 'ifelse',
  WHILE = 'while',
  SCRAPER = 'scraper',
  SWARM = 'swarm',
  START = 'start',
  USER_APPROVAL = 'user_approval',
  USER_INPUT = 'user_input',
  END = 'end',
}
